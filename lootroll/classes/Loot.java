public class Loot {

    /*
     * Loot, Class. Shouldn't have tons of subclasses.
     * 
     * -isUnique boolean (T if Unique to boss table. F if standard)
     * -String name (This might just be the identifier). Might change to a huge library of id, item pairing things
     * 
     */
}
